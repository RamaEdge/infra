apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: forgejo-db
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: forgejo
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: postgresql
      version: 18.1.13
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  values:
    # Set explicit name to have predictable service name
    fullnameOverride: forgejo-postgresql
    global:
      postgresql:
        auth:
          username: forgejo
          database: forgejo
          existingSecret: forgejo-db-credentials
          secretKeys:
            adminPasswordKey: postgres-password
            userPasswordKey: password
    primary:
      nodeAffinityPreset:
        type: "hard"
        key: "node-role.apps"
        values:
          - "true"
      podAntiAffinityPreset: hard
      persistence:
        enabled: true
        storageClass: longhorn-replicated
        size: 10Gi
      resources:
        requests:
          cpu: 250m
          memory: 512Mi
        limits:
          cpu: "1"
          memory: 1Gi
