---
phase: 03-replicated-storage
plan: "04"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - clusters/k3s-cluster/apps/smedja/namespace.yaml
  - clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
  - clusters/k3s-cluster/apps/smedja/kustomization.yaml
  - clusters/k3s-cluster/kustomization.yaml
autonomous: true
requirements:
  - STOR-06

must_haves:
  truths:
    - "A smedja namespace manifest exists in apps/smedja/"
    - "A Smedja PostgreSQL HelmRelease exists with primary PVC size 10Gi on longhorn-replicated"
    - "The Smedja app directory is registered in the top-level cluster kustomization"
    - "The HelmRelease follows the same pattern as forgejo-db and keycloak-db (Bitnami postgresql 18.1.13)"
  artifacts:
    - path: "clusters/k3s-cluster/apps/smedja/namespace.yaml"
      provides: "smedja Namespace"
      contains: "name: smedja"
    - path: "clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml"
      provides: "Smedja PostgreSQL HelmRelease"
      contains: "storageClass: longhorn-replicated"
    - path: "clusters/k3s-cluster/apps/smedja/kustomization.yaml"
      provides: "Smedja Flux Kustomization"
      contains: "postgres-helmrelease.yaml"
    - path: "clusters/k3s-cluster/kustomization.yaml"
      provides: "Top-level cluster resource list"
      contains: "apps/smedja"
  key_links:
    - from: "clusters/k3s-cluster/kustomization.yaml"
      to: "clusters/k3s-cluster/apps/smedja/kustomization.yaml"
      via: "resources entry"
      pattern: "apps/smedja"
    - from: "clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml"
      to: "longhorn-replicated StorageClass"
      via: "primary.persistence.storageClass value"
      pattern: "storageClass: longhorn-replicated"
---

<objective>
Create the apps/smedja directory with a namespace manifest, a PostgreSQL HelmRelease (10Gi on longhorn-replicated), and a kustomization — then register it in the top-level cluster kustomization. This satisfies STOR-06 and establishes the infrastructure foundation for Smedja's future full deployment.

Purpose: Smedja is not yet deployed but is referenced in requirements with a 10Gi replicated PostgreSQL PVC. Creating the postgres HelmRelease now (following the same pattern as forgejo-db and keycloak-db) satisfies STOR-06. The HelmRelease will remain in a degraded state until a Smedja secret is created imperatively on the cluster — this is expected and matches the pattern of other databases in the repo.
Output: Three new files (namespace, postgres HelmRelease, kustomization) and one updated top-level kustomization.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-replicated-storage/03-RESEARCH.md
@.planning/phases/03-replicated-storage/03-01-SUMMARY.md

<interfaces>
<!-- Reference patterns from existing apps to follow exactly -->

From clusters/k3s-cluster/apps/forgejo/namespace.yaml (pattern to follow):
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: forgejo
```

From clusters/k3s-cluster/apps/forgejo/postgres-helmrelease.yaml (full reference implementation):
```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: forgejo-db
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: forgejo
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: postgresql
      version: 18.1.13
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  values:
    fullnameOverride: forgejo-postgresql
    global:
      postgresql:
        auth:
          username: forgejo
          database: forgejo
          existingSecret: forgejo-db-credentials
          secretKeys:
            adminPasswordKey: postgres-password
            userPasswordKey: password
    primary:
      persistence:
        enabled: true
        storageClass: longhorn        # <-- Smedja uses longhorn-replicated
        size: 1Gi                     # <-- Smedja uses 10Gi
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 512Mi
```

From clusters/k3s-cluster/apps/forgejo/kustomization.yaml (pattern):
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - postgres-helmrelease.yaml
  - helmrelease.yaml
```

From clusters/k3s-cluster/kustomization.yaml (current, after plan 01 added longhorn-config):
```yaml
resources:
  - flux-system
  - apps/longhorn-config
  - apps/harbor
  - apps/keycloak
  - apps/kube-prometheus-stack
  - apps/loki
  - apps/opentelemetry-collector
  - apps/actions-runner-controller
  - apps/devpi
  - apps/forgejo
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create apps/smedja directory with namespace, PostgreSQL HelmRelease, and kustomization</name>
  <files>
    clusters/k3s-cluster/apps/smedja/namespace.yaml
    clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
    clusters/k3s-cluster/apps/smedja/kustomization.yaml
  </files>
  <action>
Create the directory `clusters/k3s-cluster/apps/smedja/` and write three files:

**File 1: namespace.yaml**
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: smedja
```

**File 2: postgres-helmrelease.yaml**

Follow the forgejo-db pattern exactly, adapted for Smedja with these differences:
- `metadata.name`: smedja-db
- `spec.targetNamespace`: smedja
- `values.fullnameOverride`: smedja-postgresql
- `values.global.postgresql.auth.username`: smedja
- `values.global.postgresql.auth.database`: smedja
- `values.global.postgresql.auth.existingSecret`: smedja-db-credentials (secret will be created imperatively on cluster later)
- `values.primary.persistence.storageClass`: longhorn-replicated
- `values.primary.persistence.size`: 10Gi

Full file content:
```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: smedja-db
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: smedja
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: postgresql
      version: 18.1.13
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  values:
    fullnameOverride: smedja-postgresql
    global:
      postgresql:
        auth:
          username: smedja
          database: smedja
          existingSecret: smedja-db-credentials
          secretKeys:
            adminPasswordKey: postgres-password
            userPasswordKey: password
    primary:
      persistence:
        enabled: true
        storageClass: longhorn-replicated
        size: 10Gi
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 512Mi
```

**File 3: kustomization.yaml**

Note: Smedja has no helmrelease.yaml yet (only postgres for now), so only list namespace and postgres HelmRelease:
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - postgres-helmrelease.yaml
```
  </action>
  <verify>
```bash
# Verify all three files exist
ls clusters/k3s-cluster/apps/smedja/

# Verify namespace name
grep 'name: smedja' clusters/k3s-cluster/apps/smedja/namespace.yaml

# Verify postgres HelmRelease key fields
grep 'name: smedja-db' clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
grep 'targetNamespace: smedja' clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
grep 'version: 18.1.13' clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
grep 'storageClass: longhorn-replicated' clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
grep 'size: 10Gi' clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
grep 'existingSecret: smedja-db-credentials' clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml

# Verify kustomization lists both files
grep 'namespace.yaml' clusters/k3s-cluster/apps/smedja/kustomization.yaml
grep 'postgres-helmrelease.yaml' clusters/k3s-cluster/apps/smedja/kustomization.yaml
```
  </verify>
  <done>
apps/smedja/ directory exists with namespace.yaml (name: smedja), postgres-helmrelease.yaml (smedja-db HelmRelease using bitnami/postgresql 18.1.13 with longhorn-replicated at 10Gi), and kustomization.yaml referencing both files. Follows forgejo-db pattern exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register apps/smedja in the top-level cluster kustomization</name>
  <files>
    clusters/k3s-cluster/kustomization.yaml
  </files>
  <action>
Edit `clusters/k3s-cluster/kustomization.yaml` to add `- apps/smedja` to the resources list. Insert it after `- apps/forgejo` to maintain alphabetical grouping of app entries:

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flux-system
  - apps/longhorn-config   # Longhorn StorageClass configuration
  - apps/harbor
  - apps/keycloak
  - apps/kube-prometheus-stack
  - apps/loki
  - apps/opentelemetry-collector
  - apps/actions-runner-controller
  - apps/devpi
  - apps/forgejo
  - apps/smedja
  # apps/minio is deployed via flux-system/minio-tenant-ks.yaml
  # which waits for minio-operator to be ready first
```

Note: apps/longhorn-config was added by plan 03-01. Do not remove it. Do not remove any other existing entries.
  </action>
  <verify>
```bash
# Verify apps/smedja is present
grep 'apps/smedja' clusters/k3s-cluster/kustomization.yaml

# Verify apps/longhorn-config from plan 01 is still present
grep 'apps/longhorn-config' clusters/k3s-cluster/kustomization.yaml

# Verify all original entries are still present
grep 'apps/harbor' clusters/k3s-cluster/kustomization.yaml
grep 'apps/forgejo' clusters/k3s-cluster/kustomization.yaml
grep 'flux-system' clusters/k3s-cluster/kustomization.yaml
```
  </verify>
  <done>
clusters/k3s-cluster/kustomization.yaml includes apps/smedja in its resources list. All existing entries including apps/longhorn-config (from plan 01) remain intact.
  </done>
</task>

</tasks>

<verification>
After both tasks, verify the complete Smedja chain and that the PostgreSQL HelmRelease is correctly configured:
```bash
echo "=== Smedja directory contents ==="
ls clusters/k3s-cluster/apps/smedja/

echo "=== Smedja PostgreSQL storage config (STOR-06) ==="
grep -n 'storageClass\|size:' clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml
# Expected: storageClass: longhorn-replicated, size: 10Gi

echo "=== Top-level kustomization (must include both longhorn-config and smedja) ==="
grep 'longhorn-config\|smedja' clusters/k3s-cluster/kustomization.yaml
```
</verification>

<success_criteria>
- clusters/k3s-cluster/apps/smedja/namespace.yaml exists with name: smedja
- clusters/k3s-cluster/apps/smedja/postgres-helmrelease.yaml exists: name=smedja-db, bitnami/postgresql 18.1.13, primary.persistence.storageClass=longhorn-replicated, size=10Gi (STOR-06)
- clusters/k3s-cluster/apps/smedja/kustomization.yaml exists referencing namespace.yaml and postgres-helmrelease.yaml
- clusters/k3s-cluster/kustomization.yaml includes apps/smedja
- STOR-06 requirement satisfied: Smedja PostgreSQL PVC (10Gi) uses longhorn-replicated
</success_criteria>

<output>
After completion, create `.planning/phases/03-replicated-storage/03-04-SUMMARY.md`
</output>
