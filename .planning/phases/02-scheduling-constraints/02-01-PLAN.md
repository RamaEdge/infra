---
phase: 02-scheduling-constraints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clusters/k3s-cluster/apps/actions-runner-controller/runner-scale-set-helmrelease.yaml
  - clusters/k3s-cluster/apps/actions-runner-controller/claude-scale-set-helmrelease.yaml
  - clusters/k3s-cluster/apps/actions-runner-controller/code-quality-runner-helmrelease.yaml
  - clusters/k3s-cluster/apps/actions-runner-controller/dependabot-runner-scale-set-helmrelease.yaml
  - clusters/k3s-cluster/apps/actions-runner-controller/modbus-runner-set.yaml
  - clusters/k3s-cluster/apps/actions-runner-controller/opcua-runner-set.yaml
autonomous: true
requirements:
  - RNRS-01
  - RNRS-02

must_haves:
  truths:
    - "All 6 runner HelmReleases have template.spec.nodeSelector with node-role: runners"
    - "All 6 runner HelmReleases have template.spec.topologySpreadConstraints with topologyKey kubernetes.io/hostname and whenUnsatisfiable ScheduleAnyway"
    - "The label key used is node-role (not role) throughout all files"
  artifacts:
    - path: "clusters/k3s-cluster/apps/actions-runner-controller/runner-scale-set-helmrelease.yaml"
      provides: "arc-runner-set scheduling constraints"
      contains: "node-role: runners"
    - path: "clusters/k3s-cluster/apps/actions-runner-controller/claude-scale-set-helmrelease.yaml"
      provides: "claude-runner-set scheduling constraints"
      contains: "node-role: runners"
    - path: "clusters/k3s-cluster/apps/actions-runner-controller/code-quality-runner-helmrelease.yaml"
      provides: "code-quality-runner scheduling constraints"
      contains: "node-role: runners"
    - path: "clusters/k3s-cluster/apps/actions-runner-controller/dependabot-runner-scale-set-helmrelease.yaml"
      provides: "dependabot runner scheduling constraints"
      contains: "node-role: runners"
    - path: "clusters/k3s-cluster/apps/actions-runner-controller/modbus-runner-set.yaml"
      provides: "modbus-runner-set scheduling constraints"
      contains: "node-role: runners"
    - path: "clusters/k3s-cluster/apps/actions-runner-controller/opcua-runner-set.yaml"
      provides: "opcua-runner-set scheduling constraints"
      contains: "node-role: runners"
  key_links:
    - from: "template.spec.nodeSelector"
      to: "node-role=runners label on nodes"
      via: "Kubernetes scheduler nodeSelector matching"
      pattern: "node-role: runners"
    - from: "template.spec.topologySpreadConstraints"
      to: "kubernetes.io/hostname topology domain"
      via: "topologyKey field"
      pattern: "topologyKey: kubernetes.io/hostname"
---

<objective>
Add nodeSelector and topologySpreadConstraints to all 6 ARC runner scale set HelmReleases, pinning runners to nodes labeled node-role=runners and spreading runner pods across distinct nodes.

Purpose: Runner pods currently have no node affinity and can land anywhere in the cluster. After this plan, runners will be isolated to dedicated runner nodes and best-effort spread so no two runners of the same set share a node.

Output: 6 modified HelmRelease files with template.spec.nodeSelector and template.spec.topologySpreadConstraints added.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scheduling-constraints/02-RESEARCH.md

<interfaces>
<!-- Current template.spec structure in runner HelmReleases (all 6 follow this pattern): -->
<!-- File: clusters/k3s-cluster/apps/actions-runner-controller/runner-scale-set-helmrelease.yaml -->
```yaml
values:
  template:
    spec:
      securityContext:
        fsGroup: 1001
      containers:
        - name: runner
          ...
      volumes:
        - name: work
          ...
```

<!-- The dependabot runner has a more complex spec with dnsPolicy, dnsConfig, initContainers, and multiple containers. -->
<!-- The nodeSelector and topologySpreadConstraints must be added INSIDE template.spec alongside securityContext, not replacing it. -->

<!-- CRITICAL label key: node-role (NOT role). REQUIREMENTS.md erroneously uses role=runners. Phase 1 locked the key as node-role. -->
<!-- CRITICAL whenUnsatisfiable: ScheduleAnyway (NOT DoNotSchedule) — runners scale to zero; hard constraints would leave runners Pending if there is only 1 runner node. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nodeSelector and topologySpreadConstraints to arc-runner-set, claude-runner-set, code-quality-runner</name>
  <files>
    clusters/k3s-cluster/apps/actions-runner-controller/runner-scale-set-helmrelease.yaml
    clusters/k3s-cluster/apps/actions-runner-controller/claude-scale-set-helmrelease.yaml
    clusters/k3s-cluster/apps/actions-runner-controller/code-quality-runner-helmrelease.yaml
  </files>
  <action>
In each of the three files, add two blocks to values.template.spec — nodeSelector and topologySpreadConstraints — placed BEFORE the existing securityContext key. All other existing content remains unchanged.

For runner-scale-set-helmrelease.yaml (runnerScaleSetName: arc-runner-set):
```yaml
values:
  # ... existing runnerScaleSetName, githubConfigUrl, etc. unchanged ...
  template:
    spec:
      nodeSelector:
        node-role: runners
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: arc-runner-set
      securityContext:
        fsGroup: 1001
      containers:
        # ... existing unchanged ...
      volumes:
        # ... existing unchanged ...
```

For claude-scale-set-helmrelease.yaml (runnerScaleSetName: claude-runner-set):
Use the same pattern with labelSelector.matchLabels.app.kubernetes.io/name: claude-runner-set

For code-quality-runner-helmrelease.yaml (runnerScaleSetName: code-quality-runner):
Use the same pattern with labelSelector.matchLabels.app.kubernetes.io/name: code-quality-runner

The labelSelector value must match the runnerScaleSetName of each HelmRelease — this is the label the gha-runner-scale-set chart applies to runner pods.
  </action>
  <verify>
    <automated>grep -l "node-role: runners" clusters/k3s-cluster/apps/actions-runner-controller/runner-scale-set-helmrelease.yaml clusters/k3s-cluster/apps/actions-runner-controller/claude-scale-set-helmrelease.yaml clusters/k3s-cluster/apps/actions-runner-controller/code-quality-runner-helmrelease.yaml | wc -l | grep -q 3 && echo "PASS: all 3 files have node-role: runners" || echo "FAIL: node-role missing" && grep -l "ScheduleAnyway" clusters/k3s-cluster/apps/actions-runner-controller/runner-scale-set-helmrelease.yaml clusters/k3s-cluster/apps/actions-runner-controller/claude-scale-set-helmrelease.yaml clusters/k3s-cluster/apps/actions-runner-controller/code-quality-runner-helmrelease.yaml | wc -l | grep -q 3 && echo "PASS: all 3 files have ScheduleAnyway" || echo "FAIL: ScheduleAnyway missing"</automated>
  </verify>
  <done>All 3 files contain nodeSelector with node-role: runners and topologySpreadConstraints with topologyKey: kubernetes.io/hostname and whenUnsatisfiable: ScheduleAnyway. The labelSelector.matchLabels.app.kubernetes.io/name matches each file's runnerScaleSetName. Existing securityContext, containers, and volumes blocks are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add nodeSelector and topologySpreadConstraints to dependabot, modbus-runner-set, opcua-runner-set</name>
  <files>
    clusters/k3s-cluster/apps/actions-runner-controller/dependabot-runner-scale-set-helmrelease.yaml
    clusters/k3s-cluster/apps/actions-runner-controller/modbus-runner-set.yaml
    clusters/k3s-cluster/apps/actions-runner-controller/opcua-runner-set.yaml
  </files>
  <action>
Apply the same nodeSelector + topologySpreadConstraints pattern to the remaining 3 runner HelmReleases.

For dependabot-runner-scale-set-helmrelease.yaml (runnerScaleSetName: dependabot):
The dependabot spec is more complex — it has dnsPolicy, dnsConfig, securityContext, initContainers, containers (runner + dind), and volumes.
Add nodeSelector and topologySpreadConstraints BEFORE the existing dnsPolicy key:
```yaml
values:
  template:
    spec:
      nodeSelector:
        node-role: runners
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: dependabot
      dnsPolicy: "None"
      dnsConfig:
        # ... existing unchanged ...
      securityContext:
        # ... existing unchanged ...
      initContainers:
        # ... existing unchanged ...
      containers:
        # ... existing unchanged ...
      volumes:
        # ... existing unchanged ...
```

For modbus-runner-set.yaml (runnerScaleSetName: modbus-runner-set):
```yaml
      nodeSelector:
        node-role: runners
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: modbus-runner-set
```

For opcua-runner-set.yaml (runnerScaleSetName: opcua-runner-set):
```yaml
      nodeSelector:
        node-role: runners
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: opcua-runner-set
```

Each labelSelector.matchLabels.app.kubernetes.io/name MUST match the runnerScaleSetName value in that file. No other existing content changes.
  </action>
  <verify>
    <automated>grep -l "node-role: runners" clusters/k3s-cluster/apps/actions-runner-controller/dependabot-runner-scale-set-helmrelease.yaml clusters/k3s-cluster/apps/actions-runner-controller/modbus-runner-set.yaml clusters/k3s-cluster/apps/actions-runner-controller/opcua-runner-set.yaml | wc -l | grep -q 3 && echo "PASS: all 3 files have node-role: runners" || echo "FAIL: node-role missing" && grep -l "ScheduleAnyway" clusters/k3s-cluster/apps/actions-runner-controller/dependabot-runner-scale-set-helmrelease.yaml clusters/k3s-cluster/apps/actions-runner-controller/modbus-runner-set.yaml clusters/k3s-cluster/apps/actions-runner-controller/opcua-runner-set.yaml | wc -l | grep -q 3 && echo "PASS: all 3 files have ScheduleAnyway" || echo "FAIL: ScheduleAnyway missing"</automated>
  </verify>
  <done>All 3 files contain nodeSelector with node-role: runners and topologySpreadConstraints with topologyKey: kubernetes.io/hostname and whenUnsatisfiable: ScheduleAnyway. The dependabot file's existing dnsPolicy, initContainers, and multi-container volumes are all preserved. All 6 runner HelmReleases across Task 1 and Task 2 now have consistent scheduling constraints.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the complete set:

```bash
# All 6 runner files have node-role: runners
grep -r "node-role: runners" clusters/k3s-cluster/apps/actions-runner-controller/ | grep -v operator

# All 6 runner files have ScheduleAnyway (not DoNotSchedule)
grep -r "ScheduleAnyway" clusters/k3s-cluster/apps/actions-runner-controller/ | grep -v operator

# Confirm no file uses wrong label key "role:" without "node-"
grep -r "^      role:" clusters/k3s-cluster/apps/actions-runner-controller/ && echo "FAIL: wrong label key found" || echo "PASS: no wrong label key"

# Each topologySpreadConstraints has the correct labelSelector name
grep -A5 "labelSelector:" clusters/k3s-cluster/apps/actions-runner-controller/runner-scale-set-helmrelease.yaml
grep -A5 "labelSelector:" clusters/k3s-cluster/apps/actions-runner-controller/claude-scale-set-helmrelease.yaml
```
</verification>

<success_criteria>
- 6 runner HelmReleases all have template.spec.nodeSelector: {node-role: runners}
- 6 runner HelmReleases all have template.spec.topologySpreadConstraints with topologyKey: kubernetes.io/hostname and whenUnsatisfiable: ScheduleAnyway
- Each topologySpreadConstraints.labelSelector.matchLabels.app.kubernetes.io/name matches the runnerScaleSetName in that file
- No file uses "role:" as the label key — only "node-role:"
- All existing spec content (securityContext, containers, volumes, dnsPolicy, initContainers) is preserved
- RNRS-01 satisfied: all runner scale sets have topologySpreadConstraints with kubernetes.io/hostname
- RNRS-02 satisfied: all runner scale sets have nodeSelector pinning to node-role=runners
</success_criteria>

<output>
After completion, create .planning/phases/02-scheduling-constraints/02-01-SUMMARY.md with:
- What was changed in each of the 6 files
- The exact scheduling pattern applied (nodeSelector key/value, topologySpreadConstraints fields)
- Note that 6 runner sets were found (not 7 as in requirements) — operator HelmRelease excluded
- Requirements addressed: RNRS-01, RNRS-02
</output>
