---
phase: 02-scheduling-constraints
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - clusters/k3s-cluster/apps/harbor/helmrelease.yaml
autonomous: true
requirements:
  - SCHED-01
  - SCHED-03

must_haves:
  truths:
    - "All Harbor application component pods (core, portal, jobservice, registry, trivy, nginx, exporter) are pinned to nodes labeled node-role=apps"
    - "Harbor internal database pod is pinned to nodes labeled node-role=apps"
    - "The label key is node-role (not role)"
    - "topologySpreadConstraints are intentionally omitted for all Harbor components — every Harbor component (core, portal, jobservice, registry, trivy, nginx, exporter, database.internal) runs at replica=1; topology spread provides no scheduling benefit at replica=1 and would add significant YAML complexity without value (per RESEARCH.md Pattern 4 rationale and Pitfall 3 note)"
    - "pod anti-affinity is intentionally omitted for Harbor's internal database (database.internal) — it is a single-replica StatefulSet; anti-affinity only constrains pod-to-pod placement and with 1 replica there are never two pods of the same DB to separate; chart support for affinity on database.internal is also unclear (per RESEARCH.md Open Question 2 recommendation)"
  artifacts:
    - path: "clusters/k3s-cluster/apps/harbor/helmrelease.yaml"
      provides: "Harbor per-component nodeSelector for all components"
      contains: "node-role: apps"
  key_links:
    - from: "core.nodeSelector, portal.nodeSelector, jobservice.nodeSelector, registry.nodeSelector, trivy.nodeSelector, nginx.nodeSelector, exporter.nodeSelector, database.internal.nodeSelector"
      to: "node-role=apps label on cluster nodes"
      via: "goharbor/harbor-helm per-component scheduling"
      pattern: "node-role: apps"
---

<objective>
Add nodeSelector to each Harbor application component in the Harbor HelmRelease, pinning all Harbor pods to nodes labeled node-role=apps. The Harbor chart requires scheduling fields to be set per-component — there is no single top-level nodeSelector.

Purpose: Harbor pods currently have no node affinity and can schedule on any node. After this plan, all Harbor component pods (core, portal, jobservice, registry, trivy, nginx, exporter, and the internal database) will be pinned to apps nodes.

Output: 1 modified harbor/helmrelease.yaml file with nodeSelector: {node-role: apps} added to each component section.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scheduling-constraints/02-RESEARCH.md

<interfaces>
<!-- Harbor chart (goharbor/harbor-helm 1.18.1) scheduling support: -->
<!--   Per-component nodeSelector, affinity, topologySpreadConstraints fields -->
<!--   Components that need nodeSelector: core, portal, jobservice, registry, trivy, nginx, exporter -->
<!--   Database component path: database.internal.nodeSelector -->
<!--   chart uses: chartmuseum (disabled), notary (disabled) — skip these -->

<!-- Current harbor helmrelease.yaml structure (abbreviated): -->
```yaml
values:
  externalURL: https://harbor.theedgeworks.ai
  imagePullPolicy: IfNotPresent

  core:
    image:
      repository: ghcr.io/octohelm/harbor/harbor-core
      tag: v2.14.0

  portal:
    image:
      repository: ghcr.io/octohelm/harbor/harbor-portal
      tag: v2.14.0

  jobservice:
    image: ...
    securityContext: { runAsUser: 10000, ... }

  registry:
    registry:
      image: ...
      securityContext: { ... }
    controller:
      image: ...

  nginx:
    image: ...

  database:
    type: internal
    internal:
      image: ...
      resources: { ... }

  redis:
    type: external
    external: ...

  exporter:
    image: ...
    resources: { ... }

  persistence: { ... }
  expose: { ... }
  trivy: { enabled: true, image: ..., resources: ..., ... }
  chartmuseum: { enabled: false }
  notary: { enabled: false }
  metrics: { ... }
```

<!-- IMPORTANT: The registry component has sub-components (registry and controller). -->
<!-- nodeSelector for the registry pod goes at the registry level (not registry.registry or registry.controller). -->
<!-- Verify: the goharbor/harbor-helm chart places nodeSelector at the component root level. -->
<!-- For registry, add nodeSelector at values.registry level (not values.registry.registry). -->

<!-- The Harbor internal database (database.internal) accepts nodeSelector directly. -->
<!-- Do NOT add affinity to database.internal — chart support is unclear and single-replica makes it low-value. -->
<!-- nodeSelector alone is sufficient for SCHED-01 for the database component. -->

<!-- chartmuseum and notary are disabled — skip them entirely. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nodeSelector to all Harbor application component sections</name>
  <files>
    clusters/k3s-cluster/apps/harbor/helmrelease.yaml
  </files>
  <action>
Add nodeSelector: {node-role: apps} to each Harbor component section in harbor/helmrelease.yaml. All existing content in each component section must be preserved — only add the nodeSelector field.

Add nodeSelector to each of these component sections:

1. core section — add nodeSelector before the image block:
```yaml
  core:
    nodeSelector:
      node-role: apps
    image:
      repository: ghcr.io/octohelm/harbor/harbor-core
      tag: v2.14.0
```

2. portal section — add nodeSelector before the image block:
```yaml
  portal:
    nodeSelector:
      node-role: apps
    image:
      repository: ghcr.io/octohelm/harbor/harbor-portal
      tag: v2.14.0
```

3. jobservice section — add nodeSelector before the image block:
```yaml
  jobservice:
    nodeSelector:
      node-role: apps
    image:
      repository: ghcr.io/octohelm/harbor/harbor-jobservice
      tag: v2.14.0
    securityContext:
      # ... existing unchanged ...
```

4. registry section — add nodeSelector at the registry root level (NOT under registry.registry or registry.controller):
```yaml
  registry:
    nodeSelector:
      node-role: apps
    registry:
      image:
        # ... existing unchanged ...
    controller:
      image:
        # ... existing unchanged ...
```

5. nginx section — add nodeSelector before the image block:
```yaml
  nginx:
    nodeSelector:
      node-role: apps
    image:
      repository: ghcr.io/octohelm/harbor/nginx-photon
      tag: v2.14.0
```

6. database.internal section — add nodeSelector before the image block:
```yaml
  database:
    type: internal
    internal:
      nodeSelector:
        node-role: apps
      image:
        repository: ghcr.io/octohelm/harbor/harbor-db
        tag: v2.14.0
      resources:
        # ... existing unchanged ...
```

7. trivy section — add nodeSelector before enabled or image (trivy has more fields):
```yaml
  trivy:
    nodeSelector:
      node-role: apps
    enabled: true
    image:
      # ... existing unchanged ...
```

8. exporter section — add nodeSelector before image:
```yaml
  exporter:
    nodeSelector:
      node-role: apps
    image:
      # ... existing unchanged ...
```

Skip chartmuseum and notary entirely — they are disabled (enabled: false) and adding scheduling to disabled components is unnecessary.

Do NOT add topologySpreadConstraints to Harbor components. Harbor has many single-replica components; topology spread at replica=1 provides no scheduling benefit and adds significant YAML complexity without value.
  </action>
  <verify>
    <automated>grep -c "node-role: apps" clusters/k3s-cluster/apps/harbor/helmrelease.yaml</automated>
  </verify>
  <done>harbor/helmrelease.yaml has nodeSelector: {node-role: apps} added to: core, portal, jobservice, registry (root level), nginx, database.internal, trivy, and exporter sections — 8 component sections total. The grep count of "node-role: apps" lines is 8. All existing fields in every component (image, securityContext, resources, persistence, etc.) are preserved. chartmuseum and notary sections are untouched.</done>
</task>

</tasks>

<verification>
After the task completes:

```bash
# Count how many times node-role: apps appears (expect 8)
grep -c "node-role: apps" clusters/k3s-cluster/apps/harbor/helmrelease.yaml
# Expected: 8

# Verify each component has it
for component in core portal jobservice registry nginx trivy exporter; do
  grep -A1 "^  $component:" clusters/k3s-cluster/apps/harbor/helmrelease.yaml | grep -q "node-role" && echo "PASS: $component" || echo "FAIL: $component missing nodeSelector"
done

# Verify database.internal has it
grep -A10 "database:" clusters/k3s-cluster/apps/harbor/helmrelease.yaml | grep "node-role" && echo "PASS: database.internal" || echo "FAIL: database.internal missing"

# Confirm no wrong label key
grep "^      role:" clusters/k3s-cluster/apps/harbor/helmrelease.yaml && echo "FAIL: wrong label key" || echo "PASS: label key correct"

# Confirm imagePullPolicy and externalURL are still present (sanity check)
grep "externalURL:" clusters/k3s-cluster/apps/harbor/helmrelease.yaml
grep "imagePullPolicy:" clusters/k3s-cluster/apps/harbor/helmrelease.yaml
```
</verification>

<success_criteria>
- harbor/helmrelease.yaml has nodeSelector: {node-role: apps} in: core, portal, jobservice, registry, nginx, database.internal, trivy, exporter — 8 component sections
- The label key is node-role (not role)
- All existing Harbor component fields (image repositories/tags, securityContext, resources, persistence, metrics) are preserved
- chartmuseum and notary sections are untouched
- redis section is untouched (external Redis, no scheduling needed)
- SCHED-01 satisfied: Harbor application and database pods have nodeSelector targeting apps node role
- SCHED-03 partially satisfied: Harbor components are nodeSelector-pinned to apps nodes (topology spread skipped — single-replica components, low value)
</success_criteria>

<output>
After completion, create .planning/phases/02-scheduling-constraints/02-04-SUMMARY.md with:
- List of all 8 Harbor component sections that received nodeSelector
- Rationale for skipping topologySpreadConstraints: single-replica components, complex YAML for no scheduling benefit
- Rationale for skipping chartmuseum/notary: disabled components
- Requirements addressed: SCHED-01 (Harbor app + DB pinned to apps), SCHED-03 (partial — Harbor pinned; topology spread not applied to single-replica components)
</output>
